[package]
name = "httpserver"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2"
uuid = { version = "1.8.0", features = ["v4"] }
mime_guess = "2.0.4"

[dev-dependencies]
reqwest = { version = "0.12.5", features = ["blocking"] }
